// import { Admin, Company, Customer, User } from "@prisma/client";

// export type TUser<type> = User & Record<TUserKey<type>, type>;
// type TUserKey<type> = type extends Company ? "company" : type extends Admin ? "admin" : type extends Customer ? "customer" : null;

// export type TFile = Express.Multer.File[];
